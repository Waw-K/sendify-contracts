{
  "name": "sendify-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Sendify — multi-chain batch token distribution.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.22.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
